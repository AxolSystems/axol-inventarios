import 'package:flutter/material.dart';

import '../../../utilities/theme/theme.dart';

class WbAddBottomSheet extends StatelessWidget {
  const WbAddBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 400,
    );
  }

}